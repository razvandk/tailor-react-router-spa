apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tailor.fullname" . }}-test-connection"
  labels:
    {{- include "tailor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tailor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
